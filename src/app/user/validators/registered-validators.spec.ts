import { RegisteredValidators } from './registered-validators';

describe('RegisteredValidators', () => {
  it('should create an instance', () => {
    expect(new RegisteredValidators()).toBeTruthy();
  });
});
